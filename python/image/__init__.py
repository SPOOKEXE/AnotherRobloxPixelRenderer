
from .compression import complete_pixel_compression#, complete_pixel_reconstruction
from .media import load_image_file_pixel_data

# import json
# img_size, pixels = load_image_file_pixel_data( 'C:\\Users\\Declan\\Documents\\GitHub\\AnotherRobloxPixelRenderer\\tests\\jghjghfd.PNG' )
# compressed = complete_pixel_compression( img_size, pixels ) #, zlib_level=None )

# with open("compressed.json", 'w') as file:
# 	file.write(json.dumps(compressed).replace(' ', ''))
